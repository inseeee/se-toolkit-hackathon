version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/habits.db:/app/habits.db
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "42002:80"
    depends_on:
      - backend
    restart: unless-stopped